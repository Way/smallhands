/// <reference types="vite/client" />

// Build stamp injected by vite.config.ts `define`. Reported in bug reports so a
// maintainer knows which deployment the player was running. See game/report.ts.
declare const __BUILD__: string;
